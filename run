#!/usr/bin/env sh
set -eu

root=$(CDPATH= cd "$(dirname "$0")" && pwd)

if [ "$#" -gt 0 ]; then
  case "$1" in
    /*) script=$1 ;;
    *) script=$PWD/$1 ;;
  esac
  shift
  set -- "$script" "$@"
fi

cd "$root"
exec "${CSOUND:-csound}" -n -d -m128 --orc src/run.orc -- "$@"
